#include <18f2550.h>
#include <string.h>
#use delay(crystal=4Mhz)

#FUSES HS
#FUSES NOPROTECT
#FUSES NOPUT
#FUSES NOWDT
#FUSES MCLR

#USE RS232(UART1, BAUD=9600, XMIT=PIN_C6, RCV=PIN_C7, PARITY=N, STOP=1, ENABLE=PIN_B0)

// Usar portas de entradas e saídas(TXD/RXD) default
#USE standard_io(a)
#USE standard_io(b)

void main()
{
   char ch;
   
   while(true)
   {
      getc();
      ch=getc();
      
      if((ch=='m')){
         output_high(pin_b3);
      }
   
   
   
   }
   
   

}
